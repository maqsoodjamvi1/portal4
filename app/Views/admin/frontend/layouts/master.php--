<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title><?= esc($title ?? 'Portal') ?></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="<?= base_url('assets/bootstrap.min.css') ?>" rel="stylesheet">
  <style>
    .app-header { background:#0d6efd; color:#fff; }
    .app-header a { color:#fff; text-decoration:none; }
  </style>
</head>
<body>
<header class="app-header py-2 mb-3">
  <div class="container d-flex justify-content-between align-items-center">
    <a href="<?= route_to('dashboard') ?>" class="fw-bold">Student/Parent Portal</a>
    <div class="d-flex gap-3 align-items-center">
      <span><?= esc($name ?? '') ?> (<?= esc($role ?? '') ?>)</span>
      <a class="btn btn-sm btn-outline-light" href="<?= route_to('logout') ?>">Logout</a>
    </div>
  </div>
</header>

<main class="container mb-5">
  <?= $this->renderSection('content') ?>
</main>
</body>
</html>
