# Deploy marketing site to timesoftsol.com
#
# Option A — Full static site (recommended, includes trial buttons everywhere):
#   Upload entire folder: marketing-site/
#   Contents: index.html, signup/index.html, .htaccess
#
# Option B — Keep Lovable/React app, add trial bar only:
#   Replace live index.html with: timesoftsol_page.html (from repo root)
#   Adds a single scrollable announcement bar (NOT fixed) — one "Start Free Trial" CTA only
#   Do NOT use old version with fixed bar + nav injection (caused logo clip & duplicate buttons)
#
# Redirect /signup (also in marketing-site/.htaccess):
#   Redirect 301 /signup https://portal4.timesoftsol.com/signup
#
# Nginx:
#   location = /signup { return 301 https://portal4.timesoftsol.com/signup; }
#
# Trial signup URL: https://portal4.timesoftsol.com/signup
